let gameCharts = require('./game');
let playerCharts = require('./player');
let groupCharts = require('./group');


module.exports = {
    game: gameCharts,
    player: playerCharts,
    group: groupCharts,
}